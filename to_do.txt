final app:

login page
leads to main page
pictures/usernames, alphabetized, of friends
by default, show logged user's library.
whenever a new friend is clicked, sort down to only include the inner join of the currently shown library with theirs.
  -- memoize friend libraries
include sort options
  -- take inner OR full join of tags, by user choice
  -- sort by order chosen

tags:
  online multiplayer
  local/splitscreen
  remote play
  controller support
  unplayed

sorts:
  logged user hours
  all selected user hours
  alphabetized
  random