import React from 'react';
import LandingPageHelper from '../helpers/LandingPageHelper';

const LandingPage = (props) => {
  return(<LandingPageHelper handler = {props.handler}/>);
}

export default LandingPage;